# 🔮 Fortune Service - 사주팔자 운세 분석

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![JavaScript](https://img.shields.io/badge/JavaScript-ES6+-yellow.svg)](https://www.ecma-international.org/)

> [!IMPORTANT]
> **이 애플리케이션은 웹 서버를 통해 실행해야 합니다.**  
> `index.html`을 직접 더블클릭하면 작동하지 않습니다.  
> 자세한 내용은 [실행 가이드](#-빠른-시작)를 참조하세요.

전통 명리학 이론을 기반으로 한 현대적인 사주팔자 분석 웹 애플리케이션입니다.

## ✨ 주요 기능

### 5단계 심층 분석
1. **천문 역법** - 60갑자 기반 시간 좌표 계산
2. **음양오행** - 5원소 에너지 분포 분석
3. **명리 구조** - 십신(Ten Gods) 관계 해석
4. **현실 연결** - 심리학적 성격 유형 매핑
5. **운세 분석** - 재물/건강/관계 운세 예측

### 특징
- ✅ **정확한 계산**: 절기(Solar Terms) 기반 정밀 사주 산출
- ✅ **사용자 친화적**: 전문 용어를 쉬운 언어로 번역
- ✅ **아름다운 UI**: 우주 테마의 현대적 디자인
- ✅ **완전한 독립성**: 서버 없이 클라이언트에서 작동
- ✅ **보안**: XSS 방지 및 입력 검증

## 🚀 빠른 시작

> [!WARNING]
> **중요**: 이 애플리케이션은 JSON 데이터를 로드하기 위해 웹 서버가 필요합니다.  
> 브라우저 보안 정책으로 인해 `file://` 프로토콜에서는 작동하지 않습니다.

### 설치 및 실행

#### 방법 1: 배치 파일 사용 (가장 쉬움) ⭐
```bash
# START_SERVER.bat 파일을 더블클릭
# 자동으로 서버가 시작되고 안내 메시지가 표시됩니다
```

#### 방법 2: Python 명령어 사용
```bash
# 1. 프로젝트 폴더로 이동
cd "c:\Users\PC\Desktop\Github Repository\fortune-service"

# 2. 웹 서버 실행
python -m http.server 8000

# 3. 브라우저에서 접속
# http://localhost:8000
```

#### 방법 3: VS Code Live Server
1. VS Code에서 프로젝트 열기
2. `index.html` 우클릭
3. "Open with Live Server" 선택

#### 방법 4: Node.js 사용
```bash
npx http-server
# 또는
npm install -g http-server
http-server
```

### 사용법
1. 브라우저에서 `http://localhost:8000` 접속
2. 성별, 생년월일, 태어난 시간 입력
3. "운세 확인하기" 버튼 클릭
4. 5단계 분석 결과 확인

## 📁 프로젝트 구조

```
fortune-service/
├── index.html              # 입력 페이지
├── result.html             # 결과 페이지
├── css/
│   └── style.css           # 스타일시트
├── js/
│   ├── core/
│   │   └── state.js        # 상태 관리
│   ├── utils/
│   │   ├── validator.js    # 입력 검증
│   │   └── sanitizer.js    # XSS 방지
│   ├── main.js             # 진입점
│   └── saju-engine.js      # 계산 엔진
├── data/
│   ├── narratives.json     # 십신 내러티브
│   ├── health-data.json    # 건강 데이터
│   └── career-advice.json  # 직업 조언
└── README.md
```

## 🔧 기술 스택

- **Frontend**: Vanilla JavaScript (ES6+)
- **Styling**: CSS3 with Animations
- **Architecture**: Modular SPA
- **Data**: JSON

## 📚 핵심 알고리즘

### 1. 60갑자 계산
```javascript
// 천간(Stems) 10개 × 지지(Branches) 12개 = 60갑자
const pillars = Ganji.calculate(year, month, day, time);
```

### 2. 십신 분석
```javascript
// 일간(Day Master)을 기준으로 다른 간지와의 관계 분석
const tenGod = TenGod.calculate(masterIdx, targetIdx, isBranch);
```

### 3. 오행 분포
```javascript
// 목(Wood), 화(Fire), 토(Earth), 금(Metal), 수(Water) 개수 계산
const ohaeng = Ohaeng.analyze(pillars);
```

## 🔐 보안

### 구현된 보안 기능
- ✅ XSS 방지 (HTML 이스케이프)
- ✅ 입력 검증 (날짜/시간/성별)
- ✅ 안전한 innerHTML 처리

### 개인정보 보호
- ✅ 서버 전송 없음 (완전 클라이언트 사이드)
- ✅ 쿠키 미사용

## 📈 로드맵

### v1.1 (진행 중)
- [x] 모듈화 리팩토링
- [x] 데이터 외부화
- [x] 에러 처리 강화
- [ ] 단위 테스트 추가

### v1.2 (계획)
- [ ] PWA 변환
- [ ] 다국어 지원
- [ ] 결과 공유 기능

## 📄 라이선스

MIT License - 자유롭게 사용하세요!

---

⭐ 이 프로젝트가 도움이 되셨다면 Star를 눌러주세요!